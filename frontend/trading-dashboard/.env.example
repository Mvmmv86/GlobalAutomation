# Backend API URL (opcional - será detectado automaticamente se não definido)
# VITE_API_URL=http://localhost:8000

# Environment
VITE_NODE_ENV=development

# Docker Environment (definir como 'true' quando rodar com Docker)
# DOCKER_ENV=true

